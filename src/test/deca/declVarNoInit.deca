/*
Test de Déclarations de Variables (Sans Initialisation)

{
    int x = 1;
    int y = 20;
    int z;
    int a;
    float b;

    z = 42;

    println(x, " ", y, " ", z);

    println(a, " ", b);

/*
    y = 2*x + 2*y; // y = 42

    x = 2*x + 2*x + 2*x + 2*x +
        2*x + 2*x + 2*x + 2*x +
        2*x + 2*x + 2*x + 2*x +
        2*x + 2*x + 2*x + 2*x +
        2*x + 2*x + 2*x + 2*x; // x = 20

    z = x + y; // z = 62
*/
}