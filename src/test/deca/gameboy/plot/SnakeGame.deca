#include "GameBoy.java"
#include "Snake.java"
{
    boolean update = false;
    Snake snake = new Snake();
    boolean a = true;
    int i=0;
    GameBoy gb = new GameBoy();
    gb.init();
    gb.setBackgroundColor(gb.DARK);
    snake.initSnake(6,6,20,18,15,15,gb,gb.LIGHT,gb.WHITE);

    while(a == true){
        update = gb.updateScreen();
         if(update){
         //input
         if(gb.keyPressed(gb.UP_KEY)){
            i = 0;
         }
         if(gb.keyPressed(gb.DOWN_KEY)){
            i = 1;
         }
         if(gb.keyPressed(gb.RIGHT_KEY)){
            i = 2;
         }
         if(gb.keyPressed(gb.LEFT_KEY)){
            i = 3;
         }
         a = snake.move(i);//en argument le input
         gb.sleep(3);
         //i = (i+2) % 4;
         }
    }
}