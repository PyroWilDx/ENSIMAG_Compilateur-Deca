#include "GameBoy.java"

{
    boolean updated = false;
    GameBoy gb = new GameBoy();
    gb.init();
    gb.setBackgroundColor(gb.BLACK);
    //gb.setTile(125, 1, 1);
    while (!updated) {
        updated = gb.updateScreen();
        //i = i + 1;
    }
}